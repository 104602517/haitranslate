  jieZhiDaoFO:    '截止到{val1}， 目前会员{val2}中心钱包已到达{val3}元（当前实际中心钱包余额），\n            若需要人工处理资金，请尽快操作！', \\components.ts 
    mODAL: '{val1}`\n      }', \\memberList.ts 
    niYiChengGongFa:    '你已成功发起{val1}个{val2}操作，失败{val3}个\n          {val4}操作,失败原因：{val5}', \\memberList.ts 
    wanJiaPLA: "玩家：{val1}{val2}` : ''\n        }", \\agentList.ts 
    niYiChengGongFa:    '你已成功发起{val1}个{val2}操作，失败{val3}个\n          {val4}操作,失败原因：{val5}', \\agentList.ts 
    gongDATA: '\n      共 {val1} 条记录，\n      第 {val2}/{val3} 页', \\rechargeMerchantSet.ts 
    oNELI:    "` : oneLine;\n    j = i * maxLength;\n  }\n  return newText;\n};\n\n/**\n * 替换list中的字段\n * @param list\n * @param targetField\n * @param resultField\n */\nconst toReplaceList = (list: any[], targetField: string, resultField: string = 'value') =>", \\utils.ts 
    jOINZi:    "`)\n    .join('&');\n};\n\n/**\n * 字符串折行\n * @param text 需要折行的字符串\n * @param maxLength 一行显示的最大长度\n */\nconst wrapText = (text: string, maxLength: number) =>", \\utils.ts 
    gPiPeiHT:    "?>', 'g'); // 匹配html标签的正则表达式，\"g\"是搜索匹配多个符合的内容\n  const msg = html.replace(re1, ''); // 执行替换成空字符\n  return msg;\n};\n\nconst trimsParams = (params: StateType) =>", \\utils.ts 
    ninShuRuDeT:    "您输入的{val1}有重复，重复{val2}为：\n      {val3}\n      {val4})` : ''}", \\utils.ts 
  